# Secrets — never commit these
.env
anthro.json
credentials.json
spotify-config.json
claude.md
ClaudeStart.bat
ClaudeStart.command
.claude

# Dependencies — regenerated via npm install
node_modules/

# Build output
build/

# Mac junk
.DS_Store

# Token files saved locally after OAuth
*.token.json

# Generated / cached data files (contain local paths or runtime state)
karma.json
monsters.json
nanoleaf-config.json
srd-library/
npcs.json
